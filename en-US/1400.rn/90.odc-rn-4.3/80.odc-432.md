# ODC V4.3.2

<main id="notice" type='notice'>
  <h4>Notice</h4>
  <ul>
  <li>Before you upgrade OceanBase Developer Center (ODC) from a version earlier than V4.2.0 to V4.3.2-BP3, you must read through the <strong>Considerations</strong> section in this topic. </li>
  <li>Before you use ODC V4.3.2-BP3, familiarize yourself with the descriptions in the <a href="../../200.odc-overview/500.product-limits.md">Limitations</a> topic. </li>
  </ul>
</main>

## Considerations

- Data sources

   - In ODC V4.2.0 and later, the concept of connection configuration is replaced with data source.

   - In ODC V4.2.0 and later, private connections are migrated to the individual workspace and exist as data sources.

   - In ODC V4.2.0 and later, public connections are migrated to the team workspace and exist as data sources.

   - In ODC V4.2.0 and later, for the read-only and read/write accounts in original public connections, only the read/write accounts are retained after the upgrade.

- User permissions

   - Starting from ODC V4.2.0, the `private_connection` and `apply_connection` roles are unavailable. In other words, the permissions of the two roles referenced by users no longer take effect after the upgrade to ODC V4.2.x.

   - Starting from ODC V4.2.0, the resource group feature is obsolete.

   - Starting from ODC V4.2.0, connection access permissions are obsolete. In ODC V4.2.x, database access control is implemented through project management.

- System settings

   - Starting from ODC V4.2.0, this feature is obsolete.

- Ticket module

   - Tickets initiated in versions earlier than ODC V4.2.0 can be viewed only on the **Initiated by Me** tab.

- Partitioning plans

   - This feature is upgraded in ODC V4.2.0. After the upgrade to ODC V4.2.x, historical partitioning plans created in versions earlier than V4.2.0 will stop running.

- SQL plans

   - This feature is upgraded in ODC V4.2.0. After the upgrade to ODC V4.2.x, historical SQL plans created in versions earlier than V4.2.0 will stop running.

- Task process

   - Starting from ODC V4.2.0, this feature is obsolete. ODC V4.2.x provides the risk level module for equivalent effects.

- Data desensitization

   - This feature is upgraded in ODC V4.2.0. After the upgrade to ODC V4.2.x, the data desensitization configurations created in versions earlier than V4.2.0 no longer take effect.

- Bastion host integration

   - In ODC V4.2.2, account integration and automatic data source configuration are supported. At present, SQL approval integration is not supported and will be supported in later versions.

- Session history

   - Starting from ODC V4.2.0, this feature is obsolete. In ODC V4.2.x, the usage status of an SQL window is saved by default.

## ODC V4.3.2-BP3

### Version information

* **Current version:** V4.3.2-BP3

* **Previous version:** V4.3.2-BP2

* **Release date:** January 03, 2025

* **Supported upgrade path:** You can directly upgrade ODC V2.0.0 and later to this version.

### Bug fixes

- Data archiving

  - Fixed the issue where values of the TINYINT data type exceed the valid value range when table data is archived from one native MySQL database to another.

  - Fixed the issue where a null pointer exception occurs due to empty values in the current column if the previous column in the table to be archived has no values.

  - Fixed the issue where archiving data from a table fails if a column of the TIMESTAMP data type in the table has no values.

- SQL window

  - Fixed the issue where a null pointer exception occurs during the execution of an SQL statement after ODC is upgraded from an earlier version to V4.3.2-BP1 in a custom environment.

## ODC V4.3.2-BP2

### Version information

* **Current version:** V4.3.2-BP2

* **Previous version:** V4.3.2-BP

* **Release date:** December 23, 2024

* **Supported upgrade path:** You can directly upgrade ODC V2.0.0 and later to this version.

### Bug fixes

- Data archiving

  - Fixed the issue where a task for archiving table data from a MySQL database to a MySQL tenant of OceanBase Database fails due to mapping errors of columns of a time type.

  - Fixed the issue where archiving a large table from an Oracle database to OceanBase Database times out because a short query timeout period is specified.

  - Fixed the issue where the specified search conditions do not take effect during the archiving of table data from a MySQL database.

  - Fixed the issue of slow queries caused by specifying a compound vector filter condition for archiving table data that meets the condition from a MySQL tenant of OceanBase Database.

- Partitioning plans

  - Fixed the issue where the database change subtask under a partitioning plan task is not in the expected state when the task error handling method is selected as **Ignore Error and Continue**.

- Lock-free schema changes

  - Fixed the issue of data loss caused by the failure to lock the specified user after the execution of a lock-free schema change task.

## ODC V4.3.2-BP

### Version information

* **Current version:** V4.3.2-BP

* **Previous version:** V4.3.2

* **Release date:** October 30, 2024

* **Supported upgrade path:** You can directly upgrade ODC V2.0.0 and later to this version.

### Bug fixes

- Data sources

  - Fixed the issue where the environment type of the data source to which the database in the project belongs is not updated accordingly after the environment type of the data source is changed.

  - Fixed the issue where an error is returned when you open the SQL development window after changing the environment type of the data source.

  - Fixed the issue where an error is returned during procedural language (PL) debugging on a database with the same name as a dropped database in the data source.

- Data archiving

  - Fixed the issue where the matching conditions specified for data archiving do not take effect.

  - Fixed the issue where a timeout error is returned during the archiving of data in OceanBase Database in Oracle mode.

  - Fixed the issue where data archiving in OceanBase Database in MySQL mode fails when the sequence of columns in the source is different from that in the destination.

- Database objects

  - Fixed the issue where the operation menu displayed after a database object in the left-side resource tree is right-clicked is too small.

## ODC V4.3.2

ODC V4.3.2 released in September 2024 enhances the following features:

- Support for logical databases: ODC V4.3.2 introduces new logical database features to provide a quick, reliable, and easy-to-use change basis for database and table sharding. The new features include logical database configuration, access permission management, logical table acquisition and creation, and execution of DDL changes.

- Archive link extension: ODC V4.3.2 adds two archive links for archiving data from an Oracle database to an Oracle tenant of OceanBase Database and from a PostgreSQL database to a MySQL tenant of OceanBase Database, on the basis of the archive link supported in ODC V4.3.1 from a MySQL database to a MySQL tenant of OceanBase Database.

- SQL development and data source management optimization: ODC V4.3.2 improves the usability of the SQL development page, data sources, and forms and optimizes more than 10 features such as the location key, resource tree objects, and SQL execution result.

For more information, see the "Feature updates" section.

### Version information

* **Current version:** V4.3.2

* **Previous version:** V4.3.1-BP

* **Release date:** September 26, 2024

* **Supported upgrade path:** You can directly upgrade ODC V2.0.0 and later to this version.

### Feature updates

- Logical databases

  - ODC V4.3.2 allows you to configure and remove logical databases.

  - ODC V4.3.2 can automatically and efficiently extract logical tables based on the topology of physical tables in the physical database.

  - ODC V4.3.2 allows you to create and configure logical tables as prompted.

  - ODC V4.3.2 allows you to quickly submit a DDL change request ticket on a logical database.

  - ODC V4.3.2 supports schema consistency check on logical tables for timely issue detection.

  - Logical databases are incorporated into the existing database and table permission management system to ensure access security for logical databases.

- Data archiving

  The following archive links are added:

  - Archive link from an Oracle database to an Oracle tenant of OceanBase Database

  - Archive link between two Oracle databases

  - Archive link from an Oracle tenant of OceanBase Database to an Oracle database

  - Archive link from a PostgreSQL database to a MySQL tenant of OceanBase Database

- Data cleanup

  The following cleanup links are added:

  - Clean up data in an Oracle database

  - Clean up data in a PostgreSQL database

- SQL check

  - ODC V4.3.2 supports a specification for checking rows affected in MySQL tenants of OceanBase Database and in MySQL data sources, so as to restrict the execution of large SQL-based changes and ensure business stability.

- Partitioning plans

  - A naming rule option that ensures the consistency between partition names and corresponding partition ranges is added under the settings related to specifying a column of the date type as the partitioning key.

- Data import

  - Client ODC allows you to import data directly from a folder.

- SQL window

  - On the SQL development page, you can click the product logo to return to the project list page, providing an entry backing to the homepage.

  - The interaction of the location key in the SQL window is optimized. The located database is automatically displayed in the visual area.

  - On the query result tab, you can directly view the DB time of SQL statements executed in the SQL window.

  - The method for sorting objects in the resource tree is optimized. By default, objects in the resource tree, such as data sources, projects, databases, tables, and views, are sorted by name.

  - You can directly initiate tickets of all types for databases in the resource tree of the SQL window.

- Data sources

  - You can quickly filter databases in the data source based on the availability status and whether a database has been assigned to a project.

  - The interaction of the initialization script for connecting to a data source is optimized. A configuration wizard is provided for common parameters.

- Ease of use

  - The interaction is optimized for all features without data such as data sources, projects, scripts, and resource tree.

  - The interaction in selecting a data source is optimized for all features, such as database O&M, sensitive column scanning, and database addition for projects.

  - For columns, such as the ticket description column, data source column, collation column, and user role column, which cannot be fully displayed in common forms such as the ticket list, database list, and user list, you can hover the pointer over such a column to show the complete content.

  - A URL of GitHub issues is provided on the feedback page in ODC Community Edition.

- Others

  - Azure Active Directory (Azure AD) is supported for single sign-on (SSO) integration.

  - The password requirements for login accounts are updated as follows: <br/>The password must be 8 to 32 characters in length and contain at least three types of the following characters: digits (0 to 9), uppercase letters (A to Z), lowercase letters (a to z), and special characters.

### Bug fixes

- Data objects

  - Fixed the issue where an error is reported when the system executes the formatted SQL statements generated for columns that contain large object (LOB) fields after a file upload.

  - Fixed the issue where the precision of TINYINT data is incorrect in MySQL mode.

  - Fixed the issue where a column of the Character Large Object (CLOB) or Binary Large Object (BLOB) data type whose value is NULL cannot be edited on the GUI.

- Change control

    - Fixed the issue where the precision of columns of the TINYINT data type in a MySQL data source is incorrect in the change SQL statement generated by a schema comparison task.

    - Fixed the issue where an error indicating no table privileges is returned because a partition name is mistakenly identified as a table name.

    - Fixed the issue where `new_table` in the `rename table old_table to new_table` statement is misjudged as the need for privileges on the `new_table` table.

    - Fixed the issue where a data mocking task may fail for MySQL 5.7.

    - Fixed the issue where the **Manual Execution** button is still available after being clicked.

    - Fixed the issue where the creation or update operations fail when too many risk identification rules are specified.

    - Fixed the issue where pages cannot be scrolled when too many risk identification rules are specified.

    - Fixed the issue where a scheduling interval of less than 10 minutes can be specified for scheduled tasks.

- Import and export

    - Fixed the issue where the SQL file that merges PL and table DDL statements fails to be imported due to errors in separating the SQL statements.

    - Fixed the issue where the export may fail due to path-related issues when you use Client ODC to export data in Windows.

    - Fixed the issue where the exported file is empty when you export the result set of a table whose name contains only lowercase letters in the Oracle mode of OceanBase Database.	

    - Fixed the issue where native MySQL data fails to be exported by using Client ODC on a Windows system.	

    - Fixed the issue where an error is reported when the type of the exported table is BigDecimal.

    - Fixed the issue where the result set export fails when the `*` or `NVL` function is used in SQL statements.

    - Fixed the issue where the result set export fails when the table contains SDO_GEOMETRY data.

- Data archiving

    - Fixed the issue where an error may be returned when you modify throttling parameters for data archiving.

    - Fixed the issue where the number of scan records displayed in the details of executed archiving tasks is not accurate.

    - Fixed the issue where the privileges of a database administrator (DBA) are required for archiving the data of Oracle tenants of OceanBase Database.

- Data sources

    - Fixed the issue where a test can still be successful even though an Oracle tenant is selected as the source `sys` tenant of OceanBase Database to be connected.

- Others

    - Fixed the issue where SSO may fail in the environment with a load balancing service deployed.
    
    - Fixed the issue where the startup parameter `ODC_APP_EXTRA_ARGS` is invalid.

### Known issues

* Client ODC cannot be installed in a directory whose name contains Chinese characters.

* The exported data file and content are incorrectly displayed when the table contains virtual columns.

* In Web ODC, the maximum size of schemas or data imported in a batch cannot exceed 2 GB. Otherwise, the upload cannot be performed.

* In Web ODC, the maximum size of schemas or data exported in a batch cannot exceed 2 GB. The excess part will be lost after the export.

* In the details of an export task, the total number of records of the exported objects is not accurate but depends on the views of OceanBase Database.

* If you do not set the `ob_enable_trace_log` parameter to `on`, only the execution plans of the SQL statements executed for the first time are displayed.

## References

- [ODC Community Edition](https://github.com/oceanbase/odc)

- [ODC Enterprise Edition](https://www.oceanbase.com/product/odc)